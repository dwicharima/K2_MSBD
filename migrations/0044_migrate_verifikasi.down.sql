-- Diminta: Penanganan rollback untuk tahap verifikasi.
-- Dipilih: Tidak ada tindakan (NO-OP) karena query verifikasi hanya membaca data tanpa mengubah skema/isi.
-- Alternatif: Menghapus objek; tidak dipilih karena tidak ada objek baru yang dibuat.

-- Tidak ada perubahan skema/data yang perlu di-rollback pada tahap verifikasi.